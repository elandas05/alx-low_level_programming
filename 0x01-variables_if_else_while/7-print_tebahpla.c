#include <stdio.h>
/**
 * main - prints the lowercase alphabet in reverse
 *
 * Return: always 0
 *
 */
int main(void)
{
	char f;

	f = 'z';
	while
		(f >= 'a') {
			putchar (f);
			f--;
		}
	putchar('\n');
	return (0);
}
